rules_version = '2';

// 놀발 데이터 관리자 - Firestore 보안 규칙
// - 읽기: 누구나 (대시보드 공개 조회)
// - 쓰기: 로그인한 사용자만 (관리자 데이터 입력/수정/삭제)
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
